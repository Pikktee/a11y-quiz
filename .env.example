# Database (SQLite-Pfad)
DATABASE_URL="file:./quiz.db"

# API-Key für POST /api/quiz/result
# Generieren: openssl rand -hex 32
QUIZ_API_KEY="change-me"

# Basis-URL für result_url im API-Response
NEXT_PUBLIC_BASE_URL="https://a11y-quiz.henrikheil.net"

NODE_ENV="development"
